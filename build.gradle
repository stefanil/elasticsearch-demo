allprojects { allproject ->

  allproject.ext.dockerContainerPrefix = 'arteria'

  group 'org.devel.elasticsearch-demo'
  version '0.0.1-SNAPSHOT'

  apply plugin: 'base'

  repositories {
    mavenLocal()
    mavenCentral()
    jcenter()
  }
}
